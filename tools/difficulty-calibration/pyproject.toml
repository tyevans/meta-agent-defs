[project]
name = "difficulty-calibration"
version = "0.1.0"
description = "Track challenge outcomes and predict optimal difficulty via logistic regression"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24.0",
    "scikit-learn>=1.3.0",
]
